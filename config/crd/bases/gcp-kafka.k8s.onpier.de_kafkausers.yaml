---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: kafkausers.gcp-kafka.k8s.onpier.de
spec:
  group: gcp-kafka.k8s.onpier.de
  names:
    kind: KafkaUser
    listKind: KafkaUserList
    plural: kafkausers
    singular: kafkauser
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Is user ready
      jsonPath: .status.ready
      name: Ready
      type: string
    - description: Google SA Email
      jsonPath: .status.saEmail
      name: Email
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: KafkaUser is the Schema for the kafka users API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KafkaUserSpec defines the desired state of User.
            properties:
              clusterAccess:
                description: ReadOnly or ReadWrite access to the whole cluster
                type: string
              serviceAccountName:
                type: string
              topicAccess:
                items:
                  properties:
                    role:
                      type: string
                    topic:
                      type: string
                  required:
                  - role
                  - topic
                  type: object
                type: array
            required:
            - serviceAccountName
            type: object
          status:
            description: KafkaUserStatus defines the observed state of User.
            properties:
              configHash:
                type: string
              error:
                type: string
              ready:
                type: boolean
              reconciliationActive:
                type: boolean
              saEmail:
                type: string
              topicAccessApplied:
                items:
                  properties:
                    role:
                      type: string
                    topic:
                      type: string
                  required:
                  - role
                  - topic
                  type: object
                type: array
            required:
            - ready
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
