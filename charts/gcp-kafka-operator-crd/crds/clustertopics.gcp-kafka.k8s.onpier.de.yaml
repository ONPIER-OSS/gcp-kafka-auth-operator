apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: clustertopics.gcp-kafka.k8s.onpier.de
spec:
  group: gcp-kafka.k8s.onpier.de
  names:
    kind: ClusterTopic
    listKind: ClusterTopicList
    plural: clustertopics
    shortNames:
      - topic
    singular: clustertopic
  scope: Cluster
  versions:
    - additionalPrinterColumns:
        - description: Is topic deletion protected
          jsonPath: .spec.deletionProtected
          name: DeletionProtected
          type: string
        - description: Is topic ready
          jsonPath: .status.ready
          name: Ready
          type: string
      name: v1alpha1
      schema:
        openAPIV3Schema:
          description: ClusterTopic is the Schema for the clustertopics API.
          properties:
            apiVersion:
              description: |-
                APIVersion defines the versioned schema of this representation of an object.
                Servers should convert recognized schemas to the latest internal value, and
                may reject unrecognized values.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: |-
                Kind is a string value representing the REST resource this object represents.
                Servers may infer this from the endpoint the client submits requests to.
                Cannot be updated.
                In CamelCase.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              description: ClusterTopicSpec defines the desired state of ClusterTopic.
              properties:
                config:
                  additionalProperties:
                    type: string
                  type: object
                deletionProtected:
                  type: boolean
                numPartitions:
                  type: integer
                replicationFactor:
                  type: integer
              required:
                - numPartitions
                - replicationFactor
              type: object
            status:
              description: ClusterTopicStatus defines the observed state of ClusterTopic.
              properties:
                ready:
                  type: boolean
              required:
                - ready
              type: object
          type: object
      served: true
      storage: true
      subresources:
        status: {}
